#include<stdio.h>
#include<math.h>
int main()
{
    int N=0,i,j,k;
    int d=N-i;
    char c,s;
    int m=sqrt(i);
    int n=sqrt(d);
    c='=',s='+';
    scanf("%d",&N);
    for(i=2;i<N;i++){
        if()
    }
    return 0;
}
